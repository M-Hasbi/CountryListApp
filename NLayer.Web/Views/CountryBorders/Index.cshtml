

@{
    ViewData["Title"] = "Index";
}
@model List<CountryBorderWithCountryDto>
<a class="btn btn-primary" asp-controller="CountryBorders" asp-action="Save">Save CountryBorders</a>
    <table class="table table-striped">
        <thead>
            <tr>
            <td>Id</td>
            <td>Name</td>
            <td>Country</td>
            <td>CreatedDate</td>
            <td>Actions</td>
            </tr>
        </thead>
        <tbody>

            @foreach (var item in Model)
            {
                <tr>
               <td>@item.Id</td>
                <td>@item.Names</td>
                   <td>@item.Country.Name</td>
                    <td>@item.CreatedDate</td>
                    <td>
                        <a class="btn btn-warning" asp-controller="CountryBorders" asp-action="Update" asp-route-id="@item.Id">Update</a>
                          <a class="btn btn-danger" asp-controller="CountryBorders" asp-action="Remove" asp-route-id="@item.Id">Delete</a>
                        </td>
                     </tr>
            }

        </tbody>



    </table>


